https://codepen.io/redwhilebaking/pen/RwpxgrG
1. What is the difference between an inline element and a block element?
    2. What happens when an element is positioned absolutely?
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
    4. Name 3 elements that are display block by default, 2 elements that are display inline by default, and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model?

    1. a block level element always stars on a new line.
    a block level element always takes up the full width available.
    a block element has a top and a bottom margin, an inline element does not
    an inline element does not start on a new line.
    an inline element only takes up as much width as necessary.

    2.An absolutely positioned element no longer exists in the normal document layout flow. Instead, it sits on its own layer separate from everything else

    3.

